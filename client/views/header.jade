header
  nav.navbar-inverse(role="navigation", ng-controller='HeaderCtrl')
    .container
      .navbar-header
        a.navbar-brand(href='/', ui-sref="list") W!
          i.fa.fa-tag
        button.navbar-toggle(type='button', ng-click="$root.menuOpen = !$root.menuOpen")
          span.sr-only
            | Toggle navigation
          i.fa.fa-bars
        form.navbar-form.city
          .form-control-wrapper
            i.fa.fa-search
            input.form-control.col-lg-8(type="text", typeahead="address.formatted_address for address in getLocation($viewValue)" typeahead-on-select="onSelect($item, $model, $label)", ng-model="$root.loc.city", ng-controller="LocationpickerCtrl", id="test123")
            //- placeholder="Type a city or location", 
            span.material-input
        a.navbar-brand(href='', ui-sref="in_promotion") Play!
        ul.nav.navbar-nav.navbar-right
          li.dropdown(dropdown, is-open="dropDownIsOpen", ng-if='!$root.isMobile && $root.user')
            a.dropdown-toggle(href='#', dropdown-toggle)
              span.avatar.pull-right
                img.img-circle(ng-src='{{ $root.user.facebook.picture }}', alt='{{ $root.user.facebook.name }}')
              span.hidden-xs.hidden-sm.hidden-m
              d {{$root.user.facebook.name}}
              i.fa.fa-chevron-down
            ul.dropdown-menu
              li
                a(href='/user/{{$root.user.facebook.id}}') My events
              li.divider
              li
                a(href="/auth/signout", target="_self" translate) Logout
                  i.fa.fa-sign-out
          li(ng-if='!$root.user')
            a.login(ng-href="/auth/facebook?redirectUrl={{$root.location.path() | encodeUri}}?{{$root.stateParams | encodeUri}}", translate) Login