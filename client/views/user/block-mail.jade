#getrewarded
 div.bottom(align='center')
  br
  br
  p.red(ng-if="blocked != false") {{::blocked.name}} is spamming you ?
  button.btn.btn-primary(ng-if="blocked != false", ng-click='addToSpam()') Block {{::blocked.name}}!
  br
  br
  br
  p.black You may disable all invitation mail if you want to
  button.btn.btn-danger(ng-click='blockAll()') Disable